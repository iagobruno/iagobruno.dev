import PageHeader from '@/components/PageHeader';
import SetupImgMap from '@/components/SetupImgMap';
import getFileLastUpdateDate from '@/lib/getFileLastUpdate';
import { LuArrowUpRight as LinkArrow } from "react-icons/lu";
import { list as devices } from '@/app/(pages)/uses/devices';

export const metadata = {
  title: 'Uses',
};

export const lastUpdate = getFileLastUpdateDate(import.meta.url);

<PageHeader
  overline="Uses"
  heading="Ferramentas que uso para trabalhar no dia a dia"
  subHeading={<SetupImgMap />}
/>

# Hardware

<ul>
  {devices.map((item) => (
    <li key={item.name}>
      {item.link ? (
        <a href={item.link} target="_blank" rel="noopener noreferrer">
          {item.name}
          <LinkArrow className="ml-1 inline" />
        </a>
      ) : (
        item.name
      )}
    </li>
  ))}
</ul>

# Software

### Ambiente de desenvolvimento

- OS: Ubuntu 22.04
- Editor: Visual Studio Code e Zed
- Navegador: Chrome
- API testing: Postman e Insomnia
- Terminal: Warp
- Tema do Editor: [Onix](https://marketplace.visualstudio.com/items?itemName=iagobruno.onix-theme) <span className="text-sm opacity-75">(Criado por mim)</span>
- Fonte: [Fira Code](https://github.com/tonsky/FiraCode) e [JetBrains Mono](https://www.jetbrains.com/lp/mono/)

### Ferramentas de AI

- OpenCode
- Codex
- GitHub Copilot
- ChatGPT

### Design

- Figma
- Affinity Designer

### Tecnologias

- Docker
- NodeJS
- Bun
- NVM
- PHP
- Laravel
- PostgreSQL
- MySQL
- MongoDB
- Redis
- Firebase Cloud Storage
- Supabase
- NextJS
- Nuxt
- Vite
- React
- Vue
- Svelte
- Tailwind CSS
- SCSS
- GSAP
- NPM / Yarn / PNPM
- Pest / Jest / Mocha

<footer className="mt-16 opacity-80">
  Atualizado em {lastUpdate}. <br />
  <div className="[&>p]:text-sm">
    Inspirado no projeto [uses.tech](https://uses.tech/) de Wes Bos.
  </div>
</footer>
